[package]
name = "npu-engine-sys"
version = "0.1.0"
edition = "2021"
description = "Rust FFI bindings to the NPU inference engine — XDNA2 NPU inference on Strix Halo"
license = "MIT"

[lib]
name = "npu_engine_sys"
crate-type = ["cdylib", "staticlib", "lib"]

[build-dependencies]
cc = "1"
